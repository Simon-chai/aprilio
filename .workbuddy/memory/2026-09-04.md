# 2026-09-04

## Agent 框架演进分析（未动代码，只出报告）
- 产出 `docs/AGENT_FRAMEWORK_EVALUATION.md`：候选框架对比报告。
- 结论：内嵌引擎 genai → **Rig**（中期迁移，ai.rs 协议不变换底座）；能力出口走 **rmcp MCP server**（Goose/Claude 等外部 agent 可指令操作应用，Goose 不进安装包）；不引入 Mastra/LangGraph.js（需 Node 运行时）、Swiftide（provider 面窄，仅借鉴其 HITL 审批）。
- 「动态感知」判断：换框架解决不了，需要自研能力清单（manifest.ts）——tools 用 `import.meta.glob` 自动扫描注册、页面动作注册表聚合为 `ui_action` 工具、MCP 工具表由 manifest 直出。
- 分阶段路线：P0 manifest+自动注册+ui_action → P1 rmcp MCP server → P2 genai→rig 迁移+Tool Server+流式 → P3 RAG（照片语义搜索/文档问答）。
- 待用户确认后再动 ROADMAP.md 和代码。

## 10:47 用户确认：弃 Goose，专注 Rig + rmcp
- `docs/AGENT_FRAMEWORK_EVALUATION.md` 重写为「Agent 技术栈方案：Rig + rmcp」：双轨架构（内嵌 rig-core / 外部 rmcp MCP server，stdio `aprilio mcp serve`）+ 能力清单 manifest 单一事实源 + P0~P4 阶段路线 + 风险清单 + 落选记录附录。
- `docs/ROADMAP.md`：选型节改为「2026-09-04 收敛」，新增第 3 步 P0~P4 待办；rmcp 从「按需」提级为采纳；Goose 标记不引入。
- `AGENTS.md` 规则索引登记「Agent 技术栈」一行。
- 新阶段顺序：P0 manifest → P1 genai→rig 迁移 → P2 Tool Server+流式 → P3 rmcp MCP server → P4 RAG(LanceDB+Ollama)。

## 11:05 P0 设计定调：借鉴 Spring 容器思想
- 用户提议自动扫描参考 Spring 组件管理。定调：核心是「能力容器」而非扫描本身——container.ts 为唯一事实源，manifest 只是容器视图。
- 映射：@Component→defineAgentTool/definePageAction 工厂；classpath 扫描→import.meta.glob（同为构建期已知+启动期注册）；@Conditional→condition(ctx) 谓词；DI→AgentToolContext 升级为能力容器；生命周期→onChange 事件广播（驱动 P2 Tool Server / P3 MCP 增量同步）。
- 刻意不做：构造器注入、AOP、Bean 作用域（组件量级十几，完整 IoC 过度设计）。
- 已同步更新 AGENT_FRAMEWORK_EVALUATION.md §4 与 ROADMAP P0 描述。

## 11:10 双语言容器定调：一个容器、两个执行面
- 用户指出能力不只 TS 层（Rust 已有 photos 命令，后续 RAG/MCP 在 Rust）。方案：工具定义带 executor: "ts"|"rust" 判别，rust 类直接路由 Tauri 命令。
- Rust 注册机制选 `inventory` crate（编译期散写、链接期收集，跨 crate；summer-rs 的 #[component] 底层即它）：新增 capabilities.rs，inventory::submit! 声明能力，启动上报 TS 容器合并。
- 框架核实：Rust 的 spring-rs 已更名 summer-rs（v0.7.x，2026-05 活跃，完整 Spring 复刻含 ComponentRegistry/DI）——与 Tauri 应用容器角色重复，借 inventory 机制不引框架。TS 的 NestJS/Midway/Ts.ED 需 Node（落选同 Mastra）；InversifyJS/Awilix 装饰器 DI 在 TC39+esbuild 下元数据残缺，不用；页面动作可搭 Vue provide/inject。
- 已更新 AGENT_FRAMEWORK_EVALUATION.md §4.4（双语言执行面）与 4.2 结构图。

## 12:44 开工落地：P0/P1/P1.5 完成（用户指令「开始推进，完善agent框架，替换成rig+rmcp方案」）
- **P0 能力容器**（TS）：新增 `src/agent/define.ts`（defineAgentTool/definePageAction 工厂，dangerous 标记）、`container.ts`（注册/去重/条件装配/onChange/确认门——dangerous 工具要求模型带 confirm:true 重调）、`manifest.ts`（import.meta.glob 扫描 tools/ 与 page-actions/ + ensureRustCapabilities 拉取 Rust 清单）、`page-action-bus.ts`（emitPageAction 事件）；三个旧工具改声明式 default export；page-actions/students.ts + settings.ts 首批动作；registry.ts 降级为容器视图兼容层；useAgent.ts 换容器装配，工具卡片 label 走 definition.label；StudentsView 挂动作监听。TS：typecheck 干净，16 文件 118 用例全绿。
- **P1 genai→rig-core**：Cargo.toml `rig-core = "=0.42.0"`（精确锁）+ `inventory = "0.3"`；ai.rs 重写——协议不变（AiMessage/AiChatParams/AiChatResult 原样），底座换 rig。**rig 0.42 API 要点（踩坑记录）**：统一 `Client::builder().api_key(k).base_url(u).build()`（from_url/new 旧签名已删，ollama 无密钥传 `rig_core::client::Nothing`）；所有 build/completion 错误需 `.map_err(|e| e.to_string())`；`Text::new(text)` 而非结构体字面量（Text 有 additional_params 字段）；`serde_json::json!` 非常量**不能**用于 inventory::submit! 的 static——parameters 改存 JSON 字符串字面量、上报时解析。
- **P1.5 Rust 能力注册**：`src-tauri/src/capabilities.rs`（RustCapability/inventory::collect!/register_capability! 宏/agent_capabilities 命令/log_inventory 对账）；photos 三命令首批注册（photos_dir 只读，import_photo/delete_photo_file dangerous）；lib.rs 挂 mod + setup 调 log_inventory + generate_handler 加 agent_capabilities。
- **环境坑（Windows）**：Git Bash 的 `/usr/bin/link.exe`（GNU）抢 MSVC link.exe——cargo 需前置 MSVC bin 到 PATH 并设 INCLUDE/LIB 环境变量（BuildTools 14.44.35207 + WinSDK 10.0.22621.0）。
- 文档同步：AGENT.md（容器架构 + TS/Rust 双执行面新增能力流程 + 确认门说明）、ROADMAP.md 第 3 步勾选 P0/P1/P1.5。
- 待办：cargo test 验证 Rust 侧（进行中）→ P2 Tool Server+流式 → P3 rmcp → P4 RAG。

## 13:25 验证收尾：P0/P1/P1.5 全绿，rig+rmcp 方案内嵌轨落地完成
- **Rust：cargo test 14 用例全绿**（ai.rs 10 + capabilities.rs 4）。TS：typecheck 干净，17 文件 121 用例全绿。
- 补强：dangerous 统一确认门移到容器装配层（container.resolveTools 给写操作包 withConfirmGate，ui_action 经 runPageAction 自带门）；Rust 桥接工具从 tags["write"] 还原 dangerous；AgentToolSpec 补 dangerous 字段；import_photo/delete_photo_file 的 schema 显式带 confirm 属性。
- rig 0.42 ToolResult 字段是 call(ToolCallId)/provider/name/content（不是 call_id/function）——测试断言用 result.wire_call_id() 与 result.name。
- 清理：删除 src-tauri/examples/ 下两个 genai 时代诊断脚本（头部自带「用后即删」）、Cargo.toml 的 autoexamples 注释；SettingsView 用户可见文案 genai→rig-core；README 路线图指引更新；providers/tauri.ts、index.ts、types.ts 注释同步。
- 教训（两次踩坑）：同一文件的多个 Edit 绝不能并行调用——并行会互相覆盖，第二次导致 capabilities.rs 的 From impl 修改丢失、编译才发现。
- 下一步：P2 rig Tool Server 对接容器 onChange + IPC Channel 流式；P3 rmcp MCP server（stdio `aprilio mcp serve`）；P4 RAG。

## 14:2x P2/P3/P4 全部落地（用户指令「继续推进P2 ~ P4」）

- **P2 流式**：ai.rs 抽 `prepare_request` + `dispatch_provider!` 宏（两条命令共用供应商映射），新增 `ai_chat_stream`（rig 0.42 的 `CompletionModel::stream` 是 trait 必有方法，直接 `model.stream(req)`；`StreamedAssistantContent::{Text, ToolCall}` 消费，futures::StreamExt）；文本增量经 `tauri::ipc::Channel<AiStreamEvent>` 推 `{type:"delta"}`，最终结果随命令返回值。TS：AgentLlm.chat 加 onDelta、AgentEvent 加 text-delta、providers/tauri.ts 用 `new Channel()` + invoke("ai_chat_stream")、useAgent 维护 live assistant 条目（tool-start 定格、settleStreamedReply 收尾）、mock 整段模拟 delta。**「Tool Server」结论：不需要**——循环每轮携带容器工具表，天然动态，Rust 侧常驻注册表是多余状态。
- **P3 rmcp**：`rmcp = "=3.2.0"`（要求 rustc≥1.88，rust-version 从 1.77.2 上调）。**rmcp 3.2 API 要点（踩坑记录）**：所有模型类型 `#[non_exhaustive]`——跨 crate 不能结构体字面量（连 `..Default::default()` 都 E0639），必须 `::default()` + 字段赋值；`call_tool` 返回 `CallToolResponse`（From<CallToolResult>），结果构造 `CallToolResult::success/error(Vec<ContentBlock>)`；`ServerInfo` = InitializeResult；list_tools 参数类型 `PaginatedRequestParams`；stdio = `rmcp::transport::io::stdio()` 元组 + `ServiceExt::serve` + `waiting()`。新文件：mcp_server.rs（5 只读工具、脱敏投影不含监护人电话/住址/生日、inventory 对账 stderr 提示、main.rs 参数路由 `mcp serve` 不启 GUI）、db.rs（sqlx 0.8.6 与插件同版，read_only 连接）、paths.rs（dirs::config_dir()/com.tauri.dev，env 覆盖 APRILIO_APP_DIR）。
- **P4 RAG**：rag.rs——Ollama `/api/embed`（reqwest rustls-tls，feature 集与 rig 对齐；默认 bge-m3 + localhost:11434）+ SQLite `embeddings` 表（Rust 管辖幂等建表，不进插件迁移）+ L2 归一化 BLOB + 暴力余弦。**LanceDB 调整为升级位**：0.38 拖 datafusion/arrow 全家桶 +25MB 违反 10MB 约束，万级条目暴力余弦 <1ms。rag_reindex/semantic_search 经 `crate::register_capability!`（跨模块调用要走 crate 路径，macro_export 只在定义文件内可裸用）自动进 TS 容器与 MCP。
- **环境坑**：上会话导出的 INCLUDE 首段写成 D: 盘 BuildTools——MSVC 实际在 C:；纯 Rust 编译不碰 C 编译器所以没暴露，ring（rustls 依赖）的 cc 编译踩雷 exit 2。正确 INCLUDE/LIB 全 C 盘。cargo 命令模板已修正。
- 验证：TS 18 文件 122 用例全绿；Rust cargo test 进行中（sqlx/rmcp/ring 首轮编译 ~10min）。
- 文档：ROADMAP P2/P3/P4 勾选（含 LanceDB 调整理由）；AGENT.md 新增 流式输出/MCP 出口/语义检索 三节 + 架构图更新；EVALUATION §2.2 改「逐请求透传」、§2.3/§5 P4 存储调整、顶部架构图同步。

## 14:35 全部验证通过（P2/P3/P4 收官）

- **Rust：cargo test 27 用例全绿**（ai 11 + capabilities 4 + rag 6 + mcp_server 3 + paths 2 等）。TS：18 文件 122 用例全绿（新增流式循环测试）。
- **TLS 大坑（重要教训）**：ring 的 C 编译在 build script 里 exit 2 且无 stderr（手动跑同一条 cl.exe 却成功）。放弃排查，改架构级方案：rig-core `default-features=false + ["reqwest","derive","native-tls"]`、自加的 reqwest 直接用 0.13 与 rig 统一为单版本、features 用 native-tls（Windows schannel）——ring/aws-lc-sys 两套 C 加密栈整个退出依赖图（lock 里 ring 成孤儿条目会被剪掉）。以后 Windows 上遇到 C 加密栈编译问题，优先换 schannel 路线。
- lib.rs 的 mcp_server 需 `pub mod`（main.rs 跨 crate 调 run_blocking）。
- MCP server 冒烟入口：`aprilio mcp serve`（外部客户端配置 command=aprilio.exe args=["mcp","serve"]）。
- 至此 ROADMAP 第 3 步 P0~P4 全部完成；剩余待办：MCP 写操作（脱敏开关+审批双前置）、云调用脱敏开关、rig evals。

## 14:50 MCP server 实测通过 + 测试基建（用户问「怎么测试改动是否生效」）
- **MCP stdio 全链路实测绿**：initialize（serverInfo/instructions 正确）→ tools/list（5 工具 + readOnlyHint 齐全）→ tools/call get_stats（structuredContent 正常，空库为 0）。命令 `app.exe mcp serve`，协议走 stdout，诊断走 stderr（rag_reindex 未上 MCP 面属设计）。
- **坑 1：cargo test 不重链 bin**——app.exe 是旧产物，`mcp serve` 路由不存在直接进 GUI（Chromium 报错 + 挂起）。改 Rust 后测 MCP 前必须 `cargo build`。
- **坑 2：Git Bash 裸跑 cargo build 链接失败**——/usr/bin/link.exe（coreutils）遮蔽 MSVC link.exe（报 `missing operand after '\377\376'`）；补 PATH 后又 LNK1181 找不到 OleAut32.lib（缺 LIB）。三件套 PATH/INCLUDE/LIB 缺一不可，已固化进脚本。
- **坑 3：多次强杀进程致 rustc 1.98 ICE**（incremental 缓存损坏，os error 5）——`rm -rf target/debug/incremental` 清掉即愈，不用 cargo clean。
- 新增 `scripts/test-mcp.sh`：cargo build + 标准 MCP 握手冒烟（本机 MSVC 环境内置，升级工具集时同步改脚本里的版本号路径）。
- 测试入口备忘：TS `npm run typecheck && npm test`；Rust `cargo test --manifest-path src-tauri/Cargo.toml`；MCP 冒烟 `bash scripts/test-mcp.sh`；流式/RAG 需 tauri dev + API key / Ollama bge-m3。

## 15:20 日志补齐（用户问「新增流程做好关键日志了吗」）+ ICE 根治
- 对照 LOGGING.md 排查：ai.rs 流式日志已齐（发起/中途失败 error!、成功 info!、推送失败 warn!）；**缺口两处**——rag.rs 失败路径全无落盘（只把 Err 返给调用方）、mcp_server.rs 工具调用层零日志。
- rag.rs：reindex/semantic_search 改「包装层记成败 + inner」结构——失败 error!（含耗时）、reindex 成功 info!（耗时/条目/模型/维度）、search 成功 debug!（**不记 query 全文，只记字符数**，防学生姓名进日志）。
- mcp_server.rs call_tool：工具调用 stderr 日志（MCP 模式无 plugin-log，log:: 宏空转）——`工具完成/失败 <name>（耗时 Xms）`，不记参数全文。实测可见：`[aprilio-mcp] 工具完成 get_stats（耗时 50ms）`。
- LOGGING.md 同步：ai 流式 / rag / mcp(stderr 通道) 记录点入清单；排查命令路径笔误修正（$env:APPDATA\...\app.log → $env:LOCALAPPDATA\com.tauri.dev\logs\aprilio.log）。
- **ICE 根治**：rustc 1.98 incremental 下一天两次 ICE，新增 `src-tauri/.cargo/config.toml` `[build] incremental = false`（清缓存只是临时解）。验证：cargo test 27 全绿 + MCP 冒烟绿。
