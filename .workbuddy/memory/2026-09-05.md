# 2026-09-05

## 学生日常表现快速记录（spec 落地）

- 按 `docs/superpowers/specs/2026-09-05-student-daily-behavior-tracking-design.md` 完成实现。
- Rust：`src-tauri/src/lib.rs` 新增 Migration v5（behavior_dimensions / student_behavior_records / behavior_comment_presets 三表 + 索引 + 4 维度种子与 16 条系统评语种子）。
- 数据层：`src/lib/db.ts` 增加 listBehaviorDimensions / createBehaviorDimension / addBehaviorRecord（写流水同时 upsert 评语沉淀，use_count 自学习闭环）/ listCommentPresets / listBehaviorRecords；MemoryStore 同步 mock；deleteStudent/clearAll 级联清理。
- AI 推荐：`src/lib/behavior-ai.ts`（parseAiComments 解析 + recommendComments，Tauri 且已配模型走 createLlm，否则回落高频预设；支持注入 llm 供测试）。
- UI：新组件 `QuickBehaviorPopover.vue`（460px 浮层、维度胶囊/倾向切换/✨AI+🕒常用气泡/Enter 保存/保存并记下一条/空评语抖动/日期限最近两周）；`StudentTable.vue` 操作列升级「+ 记表现 + 查看」并托管浮层与顶部 Toast。学生列表页与班级详情页（同用 StudentTable）都获得入口。
- 类型：`src/types/index.ts` 新增 Behavior 维度/流水/沉淀类型与 BEHAVIOR_POLARITY_LABEL；`src/lib/format.ts` 新增 localDateStr（本地时区，替代 UTC 的 todayISO 做业务日期）。
- 门禁：typecheck ✓ / vitest 290 用例全绿（新增 behavior-db、behavior-ai、behavior-popover、student-table-quick 四个测试文件）/ cargo check ✓。
- 坑：并行 Edit 同一文件时个别编辑丢失（deleteStudent 级联、format 导入），靠测试暴露后补回——同文件多处编辑尽量串行。

## 追加：评价倾向支持「中立」

- BehaviorPolarity 扩为 praise | improve | neutral；迁移 v5（未发布，直接改）与 MemoryStore 种子各加 4 条中立系统评语（每维度 1 条）。
- UI：倾向区三等分按钮，中立激活态浅灰 `bg-[#e8e8ed] text-ink`；AI 推荐提示词加中立分支（客观描述不带褒贬）。
- 门禁：typecheck / vitest 292 用例 / cargo check 全绿。

## 追加：Migration v6 补中立种子

- 复盘：上轮把中立种子直接并进 v5 并假设"v5 未发布"，但本机桌面端已执行过旧版 v5，迁移按版本号不重跑，导致真实库缺 4 条中立评语（用户实测暴露）。
- 修复：lib.rs 新增 Migration v6（seed_neutral_comment_presets），按 (dimension_id, type, content) NOT EXISTS 判重后幂等补插 4 条中立种子——新旧库均安全（新库 v5 已含，v6 跳过）。
- 教训：迁移一旦在任何真实库执行过就必须视为"已发布"，后续变更只能追加新版本，不能原地改。
- 门禁：typecheck / vitest 292 用例 / cargo check 全绿。
