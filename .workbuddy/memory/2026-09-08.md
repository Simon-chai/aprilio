# 2026-09-08

## 课表背景图铺设范围（用户反馈"几个课表位置都看不到"后补全）

- 原因：timetable_bg 原本只接了首页课表面板（且默认收起），课表页/班级课表没接。
- 新增 `profile.timetableBgSurfaceStyle`（白雾 0.86 遮罩，浅色表面共用）：
  - MyTimetableView：周课表卡片（week-timetable-surface）+ 按科目 AppCard（attrs 透传 style）
  - ClassDetailView：周网格包一层 class-timetable-surface 卡片；TimetableCalendar 新增 surfaceStyle prop
- 测试：my-timetable / class-detail / home-view 用合并式 vi.mock backgrounds
  （只覆盖 backgroundCacheUrl/backgroundsFileUrl → `cached://` 前缀）断言 style；
  timetable-calendar 测 surfaceStyle prop。全量 560 passed。
- 提醒用户的注意点：个人资料页上传后要点「保存更改」才生效（首页相机按钮是立即生效）。

## 背景图库：本地上传 + URL 缓存 + 历史索引切换

- 新模块 `src/lib/backgrounds.ts`：图库索引（localStorage `aprilio.backgrounds.v1`），
  结构 `BackgroundImage{id,kind,file,source,origin_url,name,added_at,used_at}`；按 `used_at` 倒序，
  每类上限 12 张（淘汰最久未用，最新一张保底）；同 URL 去重复用。
- 缓存分两处：本地上传沿用 `photos/`（`img_` 前缀，不动历史数据）；网络图走新 Rust 命令
  `download_background`（reqwest，绕开 WebView CORS）落到 `backgrounds/`（`bg_` 前缀）。
  解析函数按前缀分流目录：`backgroundCacheUrl` / `localBackgroundUrl`。
- Rust 新增 `src-tauri/src/backgrounds.rs`（backgrounds_dir / download_background / delete_background_file），
  tauri.conf.json assetProtocol scope 加 `$APPDATA/backgrounds/**`；删了未使用的 import_background。
- UI：新组件 `BackgroundPickerDialog.vue`（本地上传 / 粘贴 URL / 历史缩略图切换 / 移除背景）。
  **交互定稿（用户反馈后二次修改）**：不设单独入口，个人资料页「更换 / 选择图片」与首页相机按钮
  直接打开选择器，URL 输入框在弹窗里默认聚焦；ProfileView 的 pending/守卫逻辑挂到 picker 的
  `busy` 事件（imageBusy = selectionCount>0 || pickerBusy），弹窗卸载时未结算的选图由 picker
  自行 `discardSelectedProfileImage`。
- 语义变更：**换图不再删旧文件**（旧图留库可切回）；只有「显式删除」和「选了没保存就离开」才清文件。
  HomeView 面板背景改用 `saveProfileChanges`（原 `persistProfile` 不回写 profile.value，是个老 bug：换图要重启才生效）。
- 图片处理逻辑抽到 `src/lib/image.ts`（readImageAsDataUrl / fetchImageDataUrl），profile.ts 复用。
- 文档：`docs/BACKGROUNDS.md`（AGENTS.md 索引已登记）。
- 门禁：`npm run typecheck` + `npm test`（555 passed，新增 tests/backgrounds.test.ts、tests/background-picker.test.ts）、
  `cargo check --all-targets`、`cargo test`（34 passed）、`vite build` 全绿。

## 裁剪取景 + 课表比例统一（Req 4 完成）

- 裁剪窗：`src/lib/crop.ts`（纯几何：coverScale/clampOffset/panBy/zoomAt/exportRegion）
  + `ImageCropDialog.vue`（窗口固定 16:9、图片可拖/缩 1~5 倍锚点缩放），
  picker 加 `crop` prop 接线（ProfileView 课表背景、HomeView 面板相机）。
- Rust 改字节流模型：`download_background`/`read_image_bytes` 回 `{mime, base64}` 不落盘，
  新增 `save_background_data_url`（dataURL 校验 + magic bytes，12MB 上限，写 `bg_*.jpg`）。
- 16:9 统一：`profile.timetableBgSurfaceClass`（有背景图才挂 `aspect-[16/9]`，最小比例语义，
  内容更高自然长高）→ 我的课表周卡/分科卡、班级课表卡、万年历卡；首页面板随内容不强制。
- 坑：`-0` 陷阱——`exportRegion` 用 `0 - x` 而非 `-x`，vitest toEqual 区分 ±0。
- 坑：测试替身 ImageCropDialog 用 mount stubs（defineComponent+render），confirm/cancel 走 vm.$emit。
- 改进：importUrlBackground 去重提前到下载前（同 URL 复用记录不再重复下载）。
- 门禁：typecheck ✓、62 文件 575 测试 ✓、cargo test --lib 35 ✓、vite build ✓。

## 裁剪交互返工：弃自研，接 cropperjs（用户反馈）

- 用户否决了"窗口固定、移动图片"的自研取景：看不到被裁掉的部分、取图片中间要摸黑放大再拖，交互不可用。
- 重写 ImageCropDialog.vue：底层换 **cropperjs v1**（cropperjs@^1.6.2，无依赖）。
  配置 aspectRatio=16/9 / viewMode=1（图片完整可见）/ dragMode=move / background=false；
  原图完整展示+选区外自动变暗，选区可拖到任意位置含正中，边角拉伸改大小。
  对外接口不变（open/src/title/aspect → confirm/cancel），picker 零改动。
- 删 src/lib/crop.ts + tests/crop.test.ts；新增 tests/image-crop-dialog.test.ts
  （FakeCropper 替身断言初始化参数/导出参数/销毁时机）。
- 坑1：vi.mock 工厂在 import 阶段执行，替身类必须放 vi.hoisted（类声明有 TDZ）。
- 坑2：watch 无 immediate 不响应初始挂载（open=true 挂载时不会 build）→ onMounted + watch 共用 syncCropper。
- 经验：选交互控件先问"要经典交互还是自定义"，涉及可见性/取景的默认找成熟库，别自研。

## 学生档案页补「导入成绩」直达入口（用户误以为成绩导入缺失）

- 背景：用户说"还缺成绩导入"，核查发现整条链路早已落地（src/lib/scores.ts 758 行、
  ImportScoreDialog/ExamScorePanel/StudentScorePanel、db.ts+lib.rs 的 exams/exam_scores
  双实现、Agent 工具 import_score_sheet、36 个成绩用例全绿）。真正的缺口只有**入口可见性**：
  学生列表页顶栏只有「导入花名册」，导成绩必须先绕进班级详情页。
- 改动：StudentsView.vue 顶栏在「导入花名册」右侧加 `data-test="students-import-score-btn"`
  的 secondary「导入成绩」按钮（主按钮「新建学生」仍在最右）；新增 openScoreImport()
  （清空 scoreHandoff 后开对话框，复用已有的 scoreImportOpen/ImportScoreDialog）。
- 新增 tests/students-view-score-entry.test.ts（按钮顺序 + 点击开对话框且无交接表格）。
- 同步 docs/SCORE_IMPORT.md 功能入口表（学生档案页顶栏 / 班级详情页 Tab 两行）。
- 门禁：typecheck ✓、63 文件 571 测试 ✓。
- 经验：用户说"缺某个功能"时先 git log/grep 核实是否已存在——本轮属于"功能在、入口藏得深"。

### 更正：入口改错页（同一轮返工）

- 我误把「导入成绩」加到 StudentsView（学生档案页）顶栏，用户要的其实是
  **班级管理 → 班级详情 → 考试成绩页**（ExamScorePanel）那个按钮——它原本是工具行**最左**的
  图标按钮，要求挪到**行尾右侧**。学生档案页的改动已整体撤回（StudentsView.vue 回到 HEAD 状态，
  tests/students-view-score-entry.test.ts 删除）。
- 最终改动：ExamScorePanel 工具行改 `justify-between`，左侧一组（视角切换 segmented + 说明文字），
  右侧 AppIconButton「导入成绩」；空态文案「点击上方」→「点击右侧」。
- 新增断言（tests/score-view.test.ts）：工具行内 data-test 顺序 =
  [view-exams-btn, view-overview-btn, import-score-btn]，锁住"不在行首"。
- 门禁：typecheck ✓、62 文件 570 测试 ✓。
- 教训：用户说"放到右侧/参考 X 按钮位置"时，先确认**是哪个页面**再动手；不要顺手往别的页加按钮。

### 再调整：导入成绩图标定位（最终版）

- 用户要的不是"推到行尾"，而是**紧挨「按学生总览」右侧**（同一组内相邻兄弟）。
- ExamScorePanel 工具行最终结构：`<div data-test="score-toolbar" flex gap-3>`
  → 组内 `<div data-test="score-toolbar-actions" flex gap-2>`：[按考试|按学生总览] segmented +
  AppIconButton「导入成绩」；组外右边是说明文字 span。去掉了 justify-between。
- 测试断言升级：组内顺序 [view-exams-btn, view-overview-btn, import-score-btn] 且
  import 的下标 = segmented 下标 + 1（锁"紧挨"）。
- 空态文案：「点击「按学生总览」右侧的「导入成绩」图标…」。
- 门禁：typecheck ✓、62 文件 570 测试 ✓。

## 裁剪确认无反应修复（blob URL 治本）

- 现象：URL 导入进裁剪窗，点"使用这一帧"毫无反应。定位：GUI 日志（com.tauri.dev/logs）
  里 save_background_data_url 零记录 → 前端没走到 invoke → confirmCrop 三个静默 return。
- 根因：桌面端把原图字节拼成超大 dataURL（12MB 图 → 16MB base64）喂 img.src，
  WebView2 里加载失败/挂起 → cropper ready 永不触发 → 确认键一直 disabled（半透明不明显）。
- 修复：image.ts 加 base64ToBlobUrl/revokeBlobUrl；backgrounds.ts loadCropSource 改返回
  { src }（桌面端 blob: URL + 模块级 stagedSources Map 暂存字节），新增 sourceToDataUrl
  （blob→取暂存字节拼 dataURL，供直存路径）；importUrlBackground 经 sourceToDataUrl。
  ImageCropDialog 确认三处静默全部可见化（crop-confirm-error），ready 双通道
  （options.ready + img 一次性 DOM ready 事件）。
- 排查手段（复用价值）：① GUI 日志定位"到没到 Rust"；② playwright-core 连
  ms-playwright 本地 Chromium（chromium-1217）跑浏览器演示态全链路；
  ③ hash 路由入口是 /#/profile；④ attrs 透传会覆盖组件模板里的同名 data-test
  （profile-bg-picker 覆盖 bg-picker）——选择器要用未被覆盖的内部元素。
  ⑤ 第二个 app.exe 实例起不来（WebView2 数据目录锁 0x8007139F）。
- 门禁：typecheck ✓ 62 文件 571 测试 ✓ build ✓ 浏览器回归（确认→入库）✓

## 新增 docs/examples/成绩表样例.csv

- 与 花名册样例.csv 同班（春晖小学2024级三年级（2）班）10 人，学号一一对应（20240201~20240210）。
- 结构：首行标题（含「期中」关键词 → 考试名）+ 表头 姓名,学号,考试日期,语文,数学,英语,科学,道德与法治,总分。
  「考试日期」列统一 2026-11-05（scores.ts 的 EXAM_DATE_HEADER_PATTERNS 认领，优先于标题行）；
  「总分」列故意留着验证 DERIVED_HEADER_PATTERNS 汇总列不导入；吴雅静英语填「缺考」验证 grade 文字成绩。
- 验证手法（可复用）：写临时 tests/tmp-*.test.ts 调 parseRosterTable + detectScoreSheet 打印识别结果，
  跑完删掉。结果：姓名列 index 0、学号列 1、5 个科目列 from=header、examName/examDate 正确、confidence high。
- 样例 CSV 一律 UTF-8 BOM + CRLF（与既有样例一致）。

## 修复：导入对话框「智能识别中」叠加层透底显示错乱（用户截图反馈）

- 现象：AI 分析时「智能识别中」转圈与底下说明文字/按钮叠在一起，像渲染错乱。
- 根因：AnalyzingOverlay 是 `bg-canvas/85 + backdrop-blur-[2px]`（85% 半透明+2px 模糊≈没挡住），
  且挂在滚动容器内部——absolute inset-0 会随内容滚动、只圈住内容高度。
- 修复：① overlay 底色改不透明 `bg-canvas`（canvas=#fff）；② ImportScoreDialog/ImportRosterDialog
  把 overlay 挪到滚动容器外层：`relative min-h-0 flex-1` 包 `scroll-thin h-full overflow-y-auto`，
  overlay 作兄弟节点 absolute inset-0 只盖可视区。
- 测试坑：未配置 AI 时 analyzeFromTable 全程同步，overlay 一帧即消失——结构回归测试需
  vi.mock 让 aiDetectScoreSheet/aiDetectNameColumn 挂起（返回 never Promise）停在分析态；
  **vi.mock 是文件级的**，会连累同文件的正常识别用例（loadText 永不返回→超时），必须拆独立文件：
  tests/{score-import,roster-dialog}-overlay.test.ts + score-import-dialog.test.ts（识别用例）。
  断言手法：`scroll.element.contains(overlay.element) === false` 锁"不是滚动容器后代"。
- 考试名断言注意：提取的考试名回填在 `exam-name-input` 的 value 里，不在 wrapper.text()。
- 门禁：typecheck ✓、65 文件 574 测试 ✓。

### 叠加层修复返工（用户否决第一版）

- 用户反馈第一版没对症：「字体都被盖住一半了」——真正问题是**内容区太矮**：分析中只有说明文字+
  文件按钮（~100px），动画本体 ~220px，absolute inset-0 + overflow-hidden + 垂直居中 → 环和文案被上下裁掉；
  85% 半透明让底下文字透出来被边缘切过 → 视觉"盖住一半"。第一版只改不透明+挪容器，没解决高度裁切。
- 第一版已 git restore 回退。终版方案：**动画独占舞台**——滚动容器内 `v-if="analyzing"` 渲染
  `<div data-test="analyzing-stage" class="relative min-h-[320px]">` 包 AnalyzingOverlay，
  原表单内容 `v-else` 隐藏。分析中看不到内容（无叠印），舞台 min-h 保证动画完整（不裁切）。
  AnalyzingOverlay 组件本体零改动。
- 教训：修 UI 前先按截图估一下**内容与容器的高度关系**（谁比谁高、会不会被 overflow 裁），
  别一上来就改颜色/透明度；用户说"显示不全"≠"透底"，指的是内容本身被裁。
- 对应测试：tests/{score-import,roster-dialog}-overlay.test.ts 断言 stage 存在、包含动效层、
  表单按钮（选择文件/智能导入）不可见、stage 带 min-h-[320px] 类。

## 修复：导入成绩报「找不到姓名列「0」」（0 起下标误当 1 起列号）

- 根因：resolveColumn 的数字 spec 是 **1 起列号**口径（Agent 工具/报错提示一致），
  但 ImportScoreDialog.doImport 把内部的 **0 起下标** selectedColumn 直接传进去：
  姓名在第 1 列 → 传 0 → 0-1=-1 → 报错；姓名在第 2+ 列更糟——**静默错列**（下标 1 被解析到第 1 列）。
- 修复：对话框传参改 `selectedColumn.value + 1`（注释标明口径）；scores.ts/roster.ts 的
  「找不到姓名列」报错对纯数字 spec 追加「（列号从 1 开始，第 1 列请传 1）」，便于 Agent 自我纠正。
- 为何测试没拦住：scores-lib.test.ts 只传过 nameColumn:1，从没传过 0；也没有对话框点击导入的用例。
- 新增回归：① scores-lib 单测——nameColumn:0 → error 且提示含口径；nameColumn:1 → 钉住第 1 列导入成功。
  ② tests/score-import-dialog.test.ts 对话框全链路——姓名在第 1 列点「开始导入」→ 写入 4 条
  （2 学生 × 语文/数学，总分列不算）、emitted imported；清理复用 scores-lib 的 deleteExam/deleteStudent 模式。
- 坑：全量 vitest 偶发「Errors 1 error」无测试失败（复跑两轮全绿），疑瞬时未处理 rejection，先观察。
- 门禁：typecheck ✓、65 文件 576 测试 ✓。未动 Rust。
