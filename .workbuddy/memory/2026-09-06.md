# 2026-09-06

## 学生档案 / 监护人 / 回收站三项优化

- 学生档案：`enroll_date` 下线，改为 `id_card`（身份证号，选填，15/18 位正则校验）；花名册导入同步支持身份证列（表头关键词 + 18 位内容嗅探），标准模板同步替换；Agent `manage_students` 参数改 `id_card`。
- 监护人：新增 `occupation`（职业）与 `tags`（风格标签，JSON 数组字符串入库）；表单支持候选标签一键添加 + 自定义输入（去重、限 6 个）；详情页「家长联系」展示。
- 回收站：`recycle_bin` 表存快照 JSON。删除学生/班级 = 整档快照进回收站（监护人/照片/表现流水随行，落盘图片暂不删）；7 天内可恢复（重新分配 ID），过期在应用启动与打开回收站时彻底删除（连带清图片文件）。班级删除弹窗提供「删除」与「删除并重建」两个动作；侧边栏新增回收站页 `/recycle-bin`。
- 测试：新增 recycle-bin-db / recycle-bin-view 两文件；更新 guardians-db/types、class-data、behavior-popover、student-table-quick、classes-view、student-form-dialog。

## 迁移收敛（用户明确：软件从未分发，不考虑迁移逻辑）

- `src-tauri/src/lib.rs`：v1~v7 七个迁移合并为**单一 v1 初始建表**（296 行 → 227 行）。最终结构一次到位：students 含 `id_card` 且不再有 `enroll_date`/内联 `guardian_name`/`guardian_phone` 列，gender 默认值 `'male'` → `'男'`；guardians 自带 `occupation`/`tags`；photos 的 `student_id` 可空（班级公共照片）；评语种子含全部 20 条（含中立）；`recycle_bin` 一次建齐。
- `src/lib/db.ts`：清除全部为旧库兜底的空 catch 兼容垫片（listClasses / renameClass / batchUpdateStudentClass / restoreRecycleItem / deleteClass / clearAll），只保留 JSON 解析与逐张删图的合理容错。
- class-data.test.ts fixture 里遗留的 `guardian_name`/`guardian_phone` 字段清掉（tsconfig 只 include `src/**`，测试里的多余属性 typecheck 拦不住）。
- **⚠️ 坑：本地开发库必须重建。** `%APPDATA%/com.tauri.dev/aprilio.db` 已记录旧迁移版本 1~6，收敛后的 v1 不会重跑，旧库缺 `id_card`/`occupation`/`tags`/`recycle_bin` 会直接报错——删除该文件（演示数据，无损失）后启动应用即按新结构重建。
- 门禁：typecheck / vitest 345 用例 / cargo check 全绿。
