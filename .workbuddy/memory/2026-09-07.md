# 2026-09-07

## 我的课表五轮交互优化（节次/日程/类型胶囊）

- 需求：①节次一栏去掉时间显示；②备忘添加等操作集成在单元格里，删掉额外添加区；③备忘类型说明胶囊只出现一处、放课表右上角（参考超级课程表/WakeUp、Google Calendar/Trello quick-add 模式）。
- 落点 `src/views/MyTimetableView.vue`：
  - 节次列只显示序号（周网格 `week-period-cell` 与按科目 `block-period-cell` 都去时间；按科目会话格里的时间一并去掉）。
  - 周网格末行新增「日程」行（左标签 + 每天一格）：当日事件直接画在格内（勾选/悬浮删除），格内「＋ 记{类型}」行内速记——`composingDate` 状态 + `vFocus` 自定义 directive，回车提交 / Esc 取消 / 失焦有内容自动提交；`addEvent` 仅在仍停留在该格录入态时才收起（防失焦提交后误关别的格）。
  - 类型胶囊（备忘/待办/考试/作业）收拢到课表卡片右上角，图例 + 速记类型选择器双重身份；课表口径说明从底部脚注移到卡片头部左侧。
  - 删除原课表下方 5 张日程卡区（`week-event-day` → 网格内 `week-event-cell`）。
- 测试同步 `tests/my-timetable-view.test.ts`：日程行 5 格、节次格无时间（不匹配 `\d{1,2}:\d{2}`）、`event-type-pill` 恰 4 枚、新增「＋」→ 输入 → 回车落库的交互用例。typecheck + 全量测试通过（58 文件 / 490 用例）。
- `docs/TIMETABLE.md`：新增决策记录 ⑬⑭⑮、五轮实施范围行、§6.2 与验收清单同步。

## 我的课表六轮交互优化（类型下拉/筛选胶囊/AI 快速标题）

- 需求：①编辑器输入栏前加色点下拉绑定备忘类型（选项只有颜色小圆、悬浮出类型文案）；②右上类型胶囊改为筛选用——置灰未选中类型的备忘、只高亮命中格子；③已配置 AI 模型时备忘显示 AI 总结的快速浏览标题，未配置显示全文前 8 字，悬浮看全文、点击弹卡片看全文。
- 落点：
  - `src/components/EventTypeSelect.vue`（新）：色点下拉，group-hover 显现标签，点外/Esc 关闭；`src/components/MemoTitle.vue`（新）：快速标题（原生 title tooltip 全文 + 点击弹 `memo-pop` 卡片，`@click.stop` 防冒泡开格子编辑器）。
  - `src/lib/memo-ai.ts`（新）：`summarizeMemoTitle`（isTauri + isAiConfigured 才走 rig-core，失败/未配置返回 null，不写日志）+ `parseMemoTitle`（≤12 字）。`src/lib/timetable.ts` 加 `memoPreviewText`（前 8 字 + 省略号）/`eventQuickTitle` 纯函数。
  - `calendar_events` 新增 `title TEXT`：TS ensureSchema 幂等补列 + Rust lib.rs **v5 迁移**；`db.setCalendarEventTitle(id, title)` 落库一次生成永久复用；`MyTimetableView.addEvent` 速记后 `void summarize().then()` 后台回写再 reloadEvents（不阻塞连续速记）。

## 六轮返工：类型下拉改「窄指示器 + 悬浮小卡片」

- 用户反馈下拉形态不符：选项里的行内浮现文字不要了，触发钮也不要箭头——收起态与菜单项都**只放颜色小圆**（触发钮 h-7 w-5、菜单 w-8、项 h-6 w-6 居中一个 8px 色点）。
- 类型文案统一改**悬浮小卡片**（单例深色 tooltip：`bg-ink text-canvas`、fixed 定位在指示器右侧垂直居中、贴右缘翻左侧）；菜单与卡片都 `<Teleport to="body">` + fixed 定位，避免被格子编辑器溢出裁剪——VTU 下 teleport 内容要用 `document.querySelector` + `DOMWrapper` 断言。
- 去掉原生 `title`（防与自定义卡片双重提示），改 aria-label；`EventTypeSelect` 测试用例同步重写，docs 决策 ⑯ 措辞更新。typecheck + 全量 502 用例通过。
  - `MyTimetableView.vue`：胶囊 `typeFilter`（点同枚取消）+ `filterHitAt`（格子/列头 ring-1 高亮）+ `isEventDimmed`（opacity-40 置灰）；两处编辑器输入前挂 EventTypeSelect，提示文案改「色点下拉选类型 · 回车保存」。
- 测试坑：VTU 默认挂游离 DOM，document 级 mousedown 监听在单测里收不到冒泡 → 直接 `document.dispatchEvent(new MouseEvent("mousedown"))` 验证关闭；内存种子单例跨用例残留，同格断言要按内容定位而非 `get()` 首个。
- 验证：typecheck ✓、vitest 全量 59 文件/502 用例 ✓、cargo test 32 用例 ✓。`docs/TIMETABLE.md` 决策 ⑯ 重写 + 新增 ⑰。

## 环境坑：vitest 4.1.11 全挂（Windows 小写盘符）

- 症状：`npm test` 时 58 个测试文件全部 `TypeError: Cannot read properties of undefined (reading 'config')`，0 用例执行，连最小冒烟测试都挂——与本次代码改动无关。
- 根因：vitest#10692——Git Bash 会话的 `process.cwd()` 是小写 `d:\...`，`import 'vitest'` 因盘符大小写与 vite 规范化路径不一致被解析成第二份运行时实例，首个 `describe()` 即抛错。vitest 4.x 末版（4.1.11）未含修复（修复 PR #10694 只进主线）。
- 绕过：跑测试前在同一条命令里先 `cd D:/myspace/job/aprilio`（大写盘符）再 `npm test`；WorkBuddy 的 Bash 工具每次调用会重置回小写 cwd，所以 cd 和测试必须同命令。
- 永久解法（未做，留决策）：升级 vitest 5.0.0（2026-09-03 发布，主线已含修复），需回归一遍 4→5 迁移面。

## 我的课表六轮返工：日程行打回 → 点格子直接编辑（终版形态）

- 用户反馈：上一轮的「日程行 + 格内＋按钮」仍像独立操作区，要的是**点单元格直接编辑/添加备忘**。
- 终版方案（已实现）：`calendar_events` 增加 `period INTEGER` 可空节次列（前端 ensureSchema 幂等 ALTER，走 `my_subjects` 先例，未动 Rust 迁移；schema-sync 只对账表名所以无需改 lib.rs）。period=NULL = 全天/日报事件。
  - 我的课表周网格：点任意格子（日期×节次）展开**格内编辑器**——备忘落在所属格子显示（色点+内容+完成划线），编辑器内勾选/删除/回车速记（Esc 收起、点其他格切换、草稿按格保留 `draftByCell`），课程格编辑器里附「去班级→」跳转（原格子点击跳班级改为编辑器内跳转）。
  - 全天事件显示在列头（最多2条+溢出数），点列头开全天编辑器，同一套交互；列头是 div 不是 button（内嵌输入框合法）。
  - 首页今日日程、Agent query_timetable 对带节次事件附「第N节」标签。
- 测试：**固定 fake time（周三 2026-09-09）的用例必须放文件第一个**——内存演示数据以「今天」为基准且模块级单例只种一次，后种失效；索引按 period-major=(节-1)*5+列。home-view 事件 mock 字面量补 `period`。58 文件 / 491 用例全绿。
- `docs/TIMETABLE.md`：决策 ⑬⑭⑮ 重写为终版并新增 ⑯（period 列、格内编辑器、列头全天事件、类型胶囊），§2 模型、§6.2、验收清单、五轮实施范围同步。
- 教训：同一文件的多个 Edit 绝不能并行调用（会互相覆盖丢改动）；本轮全程串行。
